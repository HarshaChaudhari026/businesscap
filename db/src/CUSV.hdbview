VIEW "CUSV"
AS SELECT cc.CUSTOMER_ID , cc.CUSTOMER_NAME, cc.CUSTOMER_LOCATION, cc.CUSTOMER_PLANT_LOCATION , ce.EMPLOYEE_PLANT_LOCATION
FROM CATALOGMINI_CUSTOMERS as cc
INNER JOIN CATALOGMINI_EMPLOYEE as ce ON cc.CUSTOMER_ID = ce.EMPLOYEE_ID;